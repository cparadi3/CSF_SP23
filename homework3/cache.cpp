#include "cache.h"
#include <vector>
#include "set.h"

Cache::Cache(int numSets, int numBlocks, int numBytes) {
    for (int i = 0; i < numSets; i++) {
        for (int j = 0; j < numBlocks; j++)
            //TODO: implement constructor
            break;
            //setVector.push_back(i*j);
    }
}
